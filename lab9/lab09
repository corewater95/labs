
## Lab 09

- Name: Mustafa Alnaqishbandi 
- Email:mustafa95957@outlook.com

## Part 1 Answers

- Make sure infinity is in your Lab09 folder in GitHub

## Part 2 Answers

1. Getting started
   - Command to find the PID:ps au
   - PID of "Terminal A":787
   - PID of "Terminal B":785
2. Using `./` to run `infinity` in Terminal B
   - PID of script:  ps au | grep infinity
   - Command to kill script: kill 818
   - Effects of running the script: termina and stooped 
Using `source` to run `infinity` in Terminal B
   - PID of script:965
   - Command to kill the script:sudo kill 
   - Effects of killing the script:terminated and stopped 
4. Running `infinity` as a background job in Terminal B
   - Command to run script in background: sudo bash /root/ceg2350-corewater95/Lab09/infinity.sh
- Job ID of script:1003
   - PID of script:1004
   - Command to kill script via job id: kill 1003
   - Effects of exiting terminal: the job is going to end 
5. Run `infinity` in a `screen` or `tmux` session
   - Command(s) to run `infinity` in a screen session:after entring screen by teyping screen, "sudo bash /root/ceg2350-corewater95/Lab09/infinity.sh"
- Detach from `screen` / `tmux` session: ctrl+a then d
   - Command to show `screen` / `tmux` sessions:
   - Effects of exiting terminal: went back to main terminal 
   - Command / steps to kill the `screen` / `tmux` session:ps au" to find the PID for the screen session, then "kill screen_PID"

## Part 3 Answers

1. git branch updates
2. git checkout updates
3. vim infinity and write #comments
4. git add and commit. git push gives (fatal: The current branch updates has no upstream branch. To push the current branch and set the remote as upstream, use git push --set-upstream origin updates)

5. keep follow the steps 

6. git checkout main

7. git merge updates

8. git add and commit and push

9. confirm and exit 

10. write git branch then see waht branch is the green one


